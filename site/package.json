{
  "name": "ai-alpha-squad-landing",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build:dashboard": "npm --prefix ./dashboard install && npm --prefix ./dashboard run build",
    "predeploy": "python3 ../scripts/sync-site-version.py --bump && npm run build:dashboard && python3 ../scripts/embed-flow-diagram.py && python3 ../scripts/generate-sitemap.py",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250409.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.14.0"
  }
}
